#include <stdio.h>
#include <math.h>
#include <stdio>
#include <math>

char a;
int a, b, c, d, radio;
float x, y, z;
char w;

void main()
{
    printf("\nHola mundo");
    printf("\n============");
    printf("\nRadio = ", c);
    scanf("hola", &radio);

    radio = (3 + 5) * 8 - (10 - 4) / 2;
    x += ;
    x -= ;
    x++;
    x--;
    x *= ;
    x /= ;
    x %= ;

    z = (a + b) * c;

    if ((3 + 5) * 8 > z)
    {
        printf("\nEs mayor");
    }
    else
    {

        printf("no lo es ", c);
    }
   

    if (radio % 2 == 0)
    {
        printf("\nRadio es par");
    }
}